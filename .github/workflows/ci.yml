name: ci

on: [push, pull_request]

permissions:
  contents: read
  pull-requests: read

jobs:
  build_linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        compiler:
          - gcc:15
          - clang:22
          - clang:21
    container:
      image: "registry.gitlab.com/offa/docker-images/${{ matrix.compiler }}"
    name: "${{ matrix.compiler }}"
    steps:
      - uses: actions/checkout@main
      - uses: offa/conan-action@e9d361b93acd968d992b3bbb15b02b0beefb6b37
      - name: Build
        run: script/ci_build.sh


  build_windows:
    runs-on: windows-latest
    name: "msvc"
    steps:
      - uses: actions/checkout@main
      - uses: TheMrMilchmann/setup-msvc-dev@2900649ebaee13f31832b80ca0e9400d0cc6f337
        with:
          arch: x64
      - uses: offa/conan-action@e9d361b93acd968d992b3bbb15b02b0beefb6b37
      - name: Build
        run: script/ci_build.sh
        shell: bash


  formatting-check:
    name: "formatting"
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - uses: jidicula/clang-format-action@654a770daa28443dd111d133e4083e21c1075674
      name: "Verify formatting"
      with:
        clang-format-version: 19

